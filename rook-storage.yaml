apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: rqdao-rook-volume
spec:
  storageClassName: rook-cephfs-pacific
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 64Gi
